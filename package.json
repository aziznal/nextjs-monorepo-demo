{
  "name": "typescript-monorepo",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "start:backend": "nx start:dev foo & nx start:dev nest-app",
    "start:frontend": "nx dev nextjs-app"
  },
  "private": true,
  "devDependencies": {
    "nx": "16.3.2"
  },
  "workspaces": [
    "packages/*"
  ]
}
